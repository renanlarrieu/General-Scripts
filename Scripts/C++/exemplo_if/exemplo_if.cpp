#include<iostream>
using namespace std;

int main()
{
    bool meia_noite = false;
    bool jogo_zerado = true;

    if(meia_noite == false)
    {
        if(jogo_zerado == false)
        {
            cout << "Vou continuar jogando video game\n";
        }
        if(jogo_zerado == true)
		{
			cout << "Vou dormir pois ja zerei o jogo\n";
		}
    }

    return 0;
}
